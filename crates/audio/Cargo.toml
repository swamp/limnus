[package]
name = "swamp-audio"
version = "0.0.10"
edition = "2021"

[dependencies]
cpal = "0.15.3"
tracing = "0.1.40"

swamp-app = { path = "../app", version = "0.0.10" }
swamp-local-resource = { path = "../local-resource", version = "0.0.10" }
