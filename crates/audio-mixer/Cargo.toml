[package]
name = "limnus-audio-mixer"
version = "0.0.10"
edition = "2021"

[dependencies]
swamp-local-resource = { path = "../local-resource", version = "0.0.10" }
swamp-assets = { path = "../assets", version = "0.0.10"}
oddio = "0.7.4"
swamp-app = { path = "../app", version = "0.0.10" }
